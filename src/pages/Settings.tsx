// Settings.tsx
import React from "react";
import { Typography } from "@mui/material";

const Settings: React.FC = () => {
  return <Typography variant="h5">设置页面</Typography>;
};

export default Settings;